DocuForge is a local Python application designed to generate documents from dynamic Jinja2 templates through a Streamlit interface. The first version focuses on clean template rendering and modular architecture, while preparing future AI-assisted document generation features.

